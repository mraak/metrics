# SQLite files are the old local dev store — the deployed app talks to
# Postgres/RDS, migrated once via migrate_to_postgres.py. Don't ship them.
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Python (offline ETL — not part of the deployed app)
__pycache__/
*.py[cod]
.venv/
venv/

# Node
studio/node_modules/
studio/.next/
studio/next-env.d.ts
studio/tsconfig.tsbuildinfo

.git/
.claude/
*.log
.DS_Store
