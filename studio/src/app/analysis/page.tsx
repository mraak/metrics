import ReportFrame from '@/components/ReportFrame'

// Signal Analysis: cross-metric loudness, composites, and the auto-search
// sweep — mounted from the schema.html report shell.
export default function AnalysisPage() {
  return <ReportFrame panel="sigan" />
}
